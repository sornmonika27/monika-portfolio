"use client"; // 👈 Add this at the top

import Typewriter from "typewriter-effect";

export default function Menu() {
  return (
    <header className="sticky top-0 text-white p-[40px] w-full flex justify-between items-center shadow-lg z-50 border-transparent bg-gradient-to-r from-purple-500 via-blue-500 to-purple-500">
      <div>
        Wecome to my portfolio!
 
      </div>
    </header>
  );
}
